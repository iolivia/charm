{
  "name": "charm",
  "version": "1.0.0",
  "description": "Charm - a particle system library.",
  "main": "index.js",
  "scripts": {
    "tsc": "tsc",
    "lint": "tslint -c tslint.json src/index.ts",
    "browserify": "browserify build/index.js -o build/index-browserify.js",
    "build": "npm run tsc && npm run lint && npm run browserify",
    "build-dev": "npm run tsc && npm run browserify"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "matter-js": "^0.14.2",
    "random-number": "0.0.9"
  },
  "devDependencies": {
    "@types/matter-js": "^0.10.1",
    "tslint": "^5.10.0",
    "typescript": "^2.9.2"  
  }
}
